---
phase: 02-content-pages
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/content/config.ts
  - src/content/projects/sample-project.md
  - src/content/projects/sample-project-2.md
  - src/content/about/index.md
  - src/styles/tokens.css
  - src/styles/global.css
  - src/layouts/BaseLayout.astro
autonomous: true
requirements:
  - ABOUT-04
must_haves:
  truths:
    - "Projects schema requires type field and no longer accepts featured field"
    - "About schema requires portrait, linkedinUrl, instagramUrl fields"
    - "All sample project Markdown files have type field and no featured field"
    - "About index.md has portrait, linkedinUrl, instagramUrl in front matter"
    - "Bold 700 font weight is registered and available as CSS custom property"
    - "Every page displays footer with exact text Alexandre Galatioto (c)2026"
  artifacts:
    - path: "src/content/config.ts"
      provides: "Updated Zod schemas for projects and about collections"
      contains: "type: z.string()"
    - path: "src/styles/tokens.css"
      provides: "Bold weight token"
      contains: "--font-weight-bold"
    - path: "src/styles/global.css"
      provides: "Bold @font-face declaration"
      contains: "NeueHaasUnica-Bold.woff2"
    - path: "src/layouts/BaseLayout.astro"
      provides: "Global footer"
      contains: "site-footer"
  key_links:
    - from: "src/content/config.ts"
      to: "src/content/projects/*.md"
      via: "Zod validation at build time"
      pattern: "type:\\s+z\\.string\\(\\)"
    - from: "src/styles/global.css"
      to: "src/styles/tokens.css"
      via: "@import './tokens.css'"
      pattern: "@font-face.*font-weight:\\s*700"
---

<objective>
Update content schemas, sample content, design tokens, and BaseLayout to prepare for page construction.

Purpose: All three content pages (home, project detail, about) depend on the updated schema fields (type, portrait, social URLs), the Bold font weight token, and the global footer. This plan lands those shared foundations so page plans can execute independently in Wave 2.

Output: Updated config.ts with new schema fields, cleaned sample content, Bold 700 @font-face + token, footer in BaseLayout.
</objective>

<execution_context>
@/Users/lemondebrule/.claude/get-shit-done/workflows/execute-plan.md
@/Users/lemondebrule/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-content-pages/02-CONTEXT.md
@.planning/phases/02-content-pages/02-RESEARCH.md
@.planning/phases/02-content-pages/02-PATTERNS.md
@.planning/phases/02-content-pages/02-UI-SPEC.md

<interfaces>
<!-- Current src/content/config.ts (full file, 22 lines): -->
```typescript
import { defineCollection, z } from 'astro:content';

const projects = defineCollection({
  type: 'content',
  schema: z.object({
    title:       z.string(),
    description: z.string(),
    cover:       z.string(),
    images:      z.array(z.string()),
    order:       z.number(),
    liveUrl:     z.string().url().optional(),
    featured:    z.boolean().default(false),
  }),
});

const about = defineCollection({
  type: 'content',
  schema: z.object({}),
});

export const collections = { projects, about };
```

<!-- Current BaseLayout.astro (full file, 27 lines): -->
```astro
---
import Nav from '../components/Nav.astro';
import '../styles/global.css';

interface Props {
  title: string;
  description?: string;
}

const { title, description = 'Art director portfolio' } = Astro.props;
---

<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta name="description" content={description} />
    <title>{title}</title>
  </head>
  <body>
    <Nav />
    <main>
      <slot />
    </main>
  </body>
</html>
```

<!-- Current tokens.css line 14: -->
```css
--font-weight-medium:    500;
```
(Bold token goes after this line)

<!-- Current global.css lines 21-27 (last @font-face block): -->
```css
@font-face {
  font-family: 'Neue Haas Unica';
  src: url('/fonts/NeueHaasUnica-Italic.woff2') format('woff2');
  font-weight: 400;
  font-style: italic;
  font-display: swap;
}
```
(Bold @font-face goes after this block)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update schemas and sample content</name>
  <files>
    src/content/config.ts,
    src/content/projects/sample-project.md,
    src/content/projects/sample-project-2.md,
    src/content/about/index.md
  </files>
  <read_first>
    src/content/config.ts,
    src/content/projects/sample-project.md,
    src/content/projects/sample-project-2.md,
    src/content/about/index.md
  </read_first>
  <action>
Replace the full content of `src/content/config.ts` with:

```typescript
import { defineCollection, z } from 'astro:content';

const projects = defineCollection({
  type: 'content',
  schema: z.object({
    title:       z.string(),
    description: z.string(),
    cover:       z.string(),
    images:      z.array(z.string()),
    order:       z.number(),
    liveUrl:     z.string().url().optional(),
    type:        z.string(),
  }),
});

const about = defineCollection({
  type: 'content',
  schema: z.object({
    portrait:     z.string(),
    linkedinUrl:  z.string().url().optional(),
    instagramUrl: z.string().url().optional(),
  }),
});

export const collections = { projects, about };
```

Changes per D-16: add `type: z.string()`. Per D-18: remove `featured` entirely. Per D-17: add `portrait: z.string()` plus social URL fields to about schema.

Update `src/content/projects/sample-project.md` front matter:
- Remove the line `featured: false`
- Add `type: "Brand Identity"` after the `liveUrl` line
- Keep all other fields unchanged

Update `src/content/projects/sample-project-2.md` front matter:
- Remove the line `featured: false`
- Add `type: "Editorial Design"` after the `order` line (this file has no liveUrl)
- Keep all other fields unchanged

Replace the full content of `src/content/about/index.md` with:

```markdown
---
portrait: "/images/about/portrait.jpg"
linkedinUrl: "https://linkedin.com/in/alexandregalatioto"
instagramUrl: "https://instagram.com/alexandregalatioto"
---

Alexandre Galatioto is an art director with 10+ years of experience across brand, editorial, and digital projects.
```

This moves social links from inline Markdown body into front matter fields (per D-14, Pitfall 5 in RESEARCH.md). The body retains only the bio text -- `<Content />` will render it on the about page.
  </action>
  <verify>
    <automated>cd /Users/lemondebrule/Downloads/_Claude && grep -c "type:" src/content/config.ts | grep -q "2" && grep -c "featured" src/content/config.ts | grep -q "0" && grep "portrait" src/content/config.ts && grep "type:" src/content/projects/sample-project.md && grep "type:" src/content/projects/sample-project-2.md && grep -c "featured" src/content/projects/sample-project.md | grep -q "0" && grep "portrait:" src/content/about/index.md && grep "linkedinUrl:" src/content/about/index.md && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - src/content/config.ts contains `type:        z.string(),` in projects schema
    - src/content/config.ts does NOT contain the word `featured`
    - src/content/config.ts contains `portrait:     z.string(),` in about schema
    - src/content/config.ts contains `linkedinUrl:  z.string().url().optional(),` in about schema
    - src/content/config.ts contains `instagramUrl: z.string().url().optional(),` in about schema
    - src/content/projects/sample-project.md contains `type: "Brand Identity"` and does NOT contain `featured`
    - src/content/projects/sample-project-2.md contains `type: "Editorial Design"` and does NOT contain `featured`
    - src/content/about/index.md contains `portrait: "/images/about/portrait.jpg"` in front matter
    - src/content/about/index.md contains `linkedinUrl:` and `instagramUrl:` in front matter
    - src/content/about/index.md body does NOT contain `[LinkedIn]` inline Markdown link
  </acceptance_criteria>
  <done>All schema fields match D-16/D-17/D-18 decisions. All sample content passes Zod validation (no unrecognized keys, no missing required fields). Social links are in about front matter, not body.</done>
</task>

<task type="auto">
  <name>Task 2: Add Bold font token and global footer</name>
  <files>
    src/styles/tokens.css,
    src/styles/global.css,
    src/layouts/BaseLayout.astro
  </files>
  <read_first>
    src/styles/tokens.css,
    src/styles/global.css,
    src/layouts/BaseLayout.astro
  </read_first>
  <action>
In `src/styles/tokens.css`, add this line after the `--font-weight-medium: 500;` line (after line 14):

```css
  --font-weight-bold:      700;
```

In `src/styles/global.css`, add this @font-face block after the Italic @font-face block (after line 27, before the `/* Reset */` comment):

```css
@font-face {
  font-family: 'Neue Haas Unica';
  src: url('/fonts/NeueHaasUnica-Bold.woff2') format('woff2');
  font-weight: 700;
  font-style: normal;
  font-display: swap;
}
```

The font file `public/fonts/NeueHaasUnica-Bold.woff2` already exists in the project.

In `src/layouts/BaseLayout.astro`, insert a `<footer>` element and a `<style>` block. The footer goes between the closing `</main>` tag (currently line 25) and the closing `</body>` tag (currently line 26). The full file after edits:

```astro
---
import Nav from '../components/Nav.astro';
import '../styles/global.css';

interface Props {
  title: string;
  description?: string;
}

const { title, description = 'Art director portfolio' } = Astro.props;
---

<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta name="description" content={description} />
    <title>{title}</title>
  </head>
  <body>
    <Nav />
    <main>
      <slot />
    </main>
    <footer class="site-footer">
      <p>Alexandre Galatioto &copy;2026</p>
    </footer>
  </body>
</html>

<style>
  .site-footer {
    background-color: var(--color-surface);
    padding-block: var(--space-lg);
    padding-inline: var(--space-xl);
    text-align: center;
    font-size: var(--font-size-sm);
    font-weight: var(--font-weight-regular);
    color: var(--color-text-muted);
  }
</style>
```

Per D-15, footer text is exactly "Alexandre Galatioto (c)2026" using the HTML entity `&copy;` for the copyright symbol. Footer uses `--color-surface` (#F5F5F5) background, `--color-text-muted` (#999999) text, centered, `--font-size-sm` (14px).
  </action>
  <verify>
    <automated>cd /Users/lemondebrule/Downloads/_Claude && grep "font-weight-bold" src/styles/tokens.css && grep "NeueHaasUnica-Bold" src/styles/global.css && grep "site-footer" src/layouts/BaseLayout.astro && grep "2026" src/layouts/BaseLayout.astro && npx astro build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - src/styles/tokens.css contains `--font-weight-bold:      700;`
    - src/styles/global.css contains `@font-face` block with `NeueHaasUnica-Bold.woff2` and `font-weight: 700`
    - src/layouts/BaseLayout.astro contains `<footer class="site-footer">`
    - src/layouts/BaseLayout.astro contains `Alexandre Galatioto` and `2026` in the footer
    - src/layouts/BaseLayout.astro contains scoped style with `.site-footer` selector
    - `npx astro build` completes without errors (validates schema + content compatibility)
  </acceptance_criteria>
  <done>Bold 700 weight is declared as @font-face and as --font-weight-bold token. Footer renders on every page via BaseLayout. Astro build passes cleanly with updated schemas and content.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Content author -> build | Markdown front matter is validated by Zod at build time |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | src/content/config.ts | mitigate | Zod schema enforces required fields and types at build time -- invalid content fails the build |
| T-02-02 | Information Disclosure | about/index.md social URLs | accept | Social media URLs are intentionally public portfolio links |
</threat_model>

<verification>
- `npx astro build` exits 0 with no warnings about unrecognized keys or missing fields
- `grep -r "featured" src/content/` returns no matches
- `grep "type:" src/content/projects/sample-project.md` returns a match
- `grep "portrait:" src/content/about/index.md` returns a match
- `grep "site-footer" src/layouts/BaseLayout.astro` returns a match
- `grep "font-weight-bold" src/styles/tokens.css` returns a match
</verification>

<success_criteria>
Schema updated with type/portrait/social fields, featured removed. All sample content files pass Zod validation. Bold font weight available as CSS token. Footer visible on all pages via BaseLayout.
</success_criteria>

<output>
After completion, create `.planning/phases/02-content-pages/02-01-SUMMARY.md`
</output>
