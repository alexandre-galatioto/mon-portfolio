---
phase: 01-foundation
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - src/layouts/BaseLayout.astro
  - src/components/Nav.astro
  - src/pages/index.astro
autonomous: false
requirements:
  - NAV-01
  - NAV-02
  - NAV-03
  - NAV-04
  - DSGN-04
  - PERF-02
  - PERF-03

must_haves:
  truths:
    - "Navigation bar renders on every page with name left, Work/About links right"
    - "Navigation hides on scroll down past 80px and reappears on scroll up"
    - "On mobile (<=768px), nav links are hidden and hamburger icon appears"
    - "Tapping hamburger opens full-screen overlay with centered Work/About links"
    - "Tapping close button or overlay link closes the overlay"
    - "npm run dev starts the site with zero errors and the nav is visible"
    - "No client-side JS bundle exists — only inline script in Nav.astro"
    - "No analytics scripts or third-party tags in the HTML output"
  artifacts:
    - path: "src/layouts/BaseLayout.astro"
      provides: "Root layout with Nav, meta tags, global CSS import"
      contains: "Nav"
    - path: "src/components/Nav.astro"
      provides: "Navigation with scroll hide/show and mobile hamburger"
      contains: "nav-hamburger"
    - path: "src/pages/index.astro"
      provides: "Home page stub using BaseLayout"
      contains: "BaseLayout"
  key_links:
    - from: "src/layouts/BaseLayout.astro"
      to: "src/styles/global.css"
      via: "import '../styles/global.css'"
      pattern: "import.*styles/global"
    - from: "src/layouts/BaseLayout.astro"
      to: "src/components/Nav.astro"
      via: "import Nav from '../components/Nav.astro'"
      pattern: "import Nav"
    - from: "src/pages/index.astro"
      to: "src/layouts/BaseLayout.astro"
      via: "import BaseLayout"
      pattern: "import BaseLayout"
    - from: "src/components/Nav.astro"
      to: "CSS tokens"
      via: "var(--nav-height), var(--space-lg), etc."
      pattern: "var\\(--"
---

<objective>
Build the BaseLayout, Navigation component (with scroll hide/show and mobile hamburger overlay), and home page stub so that `npm run dev` serves a navigable skeleton.

Purpose: Deliver the interactive navigation shell that Phase 2 pages will inherit, satisfying all NAV requirements and confirming PERF-02 (no JS bundle) and DSGN-04 (no decorative animations).

Output: A working dev server showing the nav bar with name/logo left, Work/About right, scroll hide/show behavior, and mobile hamburger overlay.
</objective>

<execution_context>
@/Users/lemondebrule/.claude/get-shit-done/workflows/execute-plan.md
@/Users/lemondebrule/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-PATTERNS.md
@.planning/phases/01-foundation/01-UI-SPEC.md
@.planning/phases/01-foundation/01-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 outputs that this plan consumes -->

From src/styles/tokens.css (CSS custom properties available globally):
  --font-family-base, --font-size-sm, --font-size-base, --font-size-lg
  --font-weight-regular, --font-weight-medium
  --color-bg, --color-text, --color-accent
  --space-sm, --space-lg, --space-xl
  --nav-height, --touch-target

From src/styles/global.css:
  @import './tokens.css' (first line)
  @font-face declarations for Regular (400), Medium (500), Italic (400i)
  Reset: box-sizing border-box, margin/padding 0
  Base: html uses var(--font-family-base), var(--color-text), var(--color-bg)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: BaseLayout and home page stub</name>
  <files>src/layouts/BaseLayout.astro, src/pages/index.astro</files>
  <read_first>
    .planning/phases/01-foundation/01-PATTERNS.md
    src/styles/global.css
  </read_first>
  <action>
1. Create `src/layouts/BaseLayout.astro` with this exact content:
```astro
---
import Nav from '../components/Nav.astro';
import '../styles/global.css';

interface Props {
  title: string;
  description?: string;
}

const { title, description = 'Art director portfolio' } = Astro.props;
---

&lt;!doctype html&gt;
&lt;html lang="en"&gt;
  &lt;head&gt;
    &lt;meta charset="UTF-8" /&gt;
    &lt;meta name="viewport" content="width=device-width, initial-scale=1.0" /&gt;
    &lt;meta name="description" content={description} /&gt;
    &lt;title&gt;{title}&lt;/title&gt;
  &lt;/head&gt;
  &lt;body&gt;
    &lt;Nav /&gt;
    &lt;main&gt;
      &lt;slot /&gt;
    &lt;/main&gt;
  &lt;/body&gt;
&lt;/html&gt;
```

Key points:
- `import '../styles/global.css'` in the frontmatter is the single import point for all global styles (D-07). This triggers the tokens.css -> global.css chain.
- `<Nav />` renders the navigation on every page that uses this layout (NAV-04).
- `<slot />` is the Astro content projection mechanism.
- No analytics scripts, no third-party tags (PERF-03).
- Props: `title` (required string), `description` (optional, defaults to "Art director portfolio").

2. Create `src/pages/index.astro` as a minimal stub:
```astro
---
import BaseLayout from '../layouts/BaseLayout.astro';
---

&lt;BaseLayout title="Work — Alexandre Galatioto"&gt;
  &lt;!-- Home page content built in Phase 2 --&gt;
&lt;/BaseLayout&gt;
```

This maps to the `/` route by Astro's file-based routing. Phase 2 replaces the comment with the project grid.
  </action>
  <verify>
    <automated>cd /Users/lemondebrule/Downloads/_Claude && grep "import Nav from" src/layouts/BaseLayout.astro && grep "import.*global.css" src/layouts/BaseLayout.astro && grep "import BaseLayout" src/pages/index.astro</automated>
  </verify>
  <acceptance_criteria>
    - src/layouts/BaseLayout.astro contains `import Nav from '../components/Nav.astro'`
    - src/layouts/BaseLayout.astro contains `import '../styles/global.css'`
    - src/layouts/BaseLayout.astro contains `<meta name="viewport"` with `width=device-width, initial-scale=1.0`
    - src/layouts/BaseLayout.astro contains `<Nav />`
    - src/layouts/BaseLayout.astro contains `<slot />`
    - src/layouts/BaseLayout.astro contains `lang="en"` on the html element
    - src/layouts/BaseLayout.astro does NOT contain `analytics`, `gtag`, `script src=`
    - src/pages/index.astro contains `import BaseLayout from '../layouts/BaseLayout.astro'`
    - src/pages/index.astro contains `title="Work`
  </acceptance_criteria>
  <done>BaseLayout imports global CSS and Nav, provides slot for page content, sets proper meta tags. Index page stub uses BaseLayout.</done>
</task>

<task type="auto">
  <name>Task 2: Nav component with scroll hide/show and mobile hamburger</name>
  <files>src/components/Nav.astro</files>
  <read_first>
    .planning/phases/01-foundation/01-PATTERNS.md
    .planning/phases/01-foundation/01-UI-SPEC.md
    src/styles/tokens.css
  </read_first>
  <action>
Create `src/components/Nav.astro` implementing the full navigation component from the PATTERNS.md Nav pattern. The file has three sections: HTML template, scoped style block, inline script block.

**HTML structure:**
```html
<nav class="site-nav" id="site-nav">
  <a href="/" class="nav-logo">Alexandre Galatioto</a>
  <ul class="nav-links">
    <li><a href="/work">Work</a></li>
    <li><a href="/about">About</a></li>
  </ul>
  <button class="nav-hamburger" aria-label="Open menu" aria-expanded="false" id="nav-hamburger">
    <span class="bar"></span>
    <span class="bar"></span>
    <span class="bar"></span>
  </button>
</nav>

<div class="nav-overlay" id="nav-overlay" aria-hidden="true">
  <button class="nav-close" aria-label="Close menu" id="nav-close">
    <span class="bar bar--close-1"></span>
    <span class="bar bar--close-2"></span>
  </button>
  <ul class="overlay-links">
    <li><a href="/work">Work</a></li>
    <li><a href="/about">About</a></li>
  </ul>
</div>
```

NAV-01: Name/logo left (`<a href="/" class="nav-logo">Alexandre Galatioto</a>`), Work/About links right (`<ul class="nav-links">`).
NAV-03: Hamburger button with `aria-label="Open menu"` and `aria-expanded="false"`.
NAV-04: Component is imported by BaseLayout, so it appears on all pages.

**Scoped `<style>` block** — copy the COMPLETE style block from PATTERNS.md Nav pattern (lines 251-382). Key CSS values:
- `.site-nav`: `position: sticky; top: 0; z-index: 100; height: var(--nav-height); padding: 0 var(--space-lg); background: var(--color-bg); display: flex; align-items: center; justify-content: space-between; transition: transform 200ms ease;`
- `.site-nav.nav--hidden`: `transform: translateY(calc(-1 * var(--nav-height)));`
- `.nav-logo`: `font-size: var(--font-size-base); font-weight: var(--font-weight-medium); color: var(--color-text); text-decoration: none;`
- `.nav-links`: `display: flex; gap: var(--space-lg); list-style: none; margin: 0; padding: 0;`
- `.nav-links a`: `font-size: var(--font-size-sm); font-weight: var(--font-weight-medium); color: var(--color-text); text-decoration: none;`
- `.nav-links a:hover`: `opacity: 0.6;`
- `.nav-links a[aria-current="page"]`: `border-bottom: 2px solid var(--color-accent); padding-bottom: 2px;`
- `.nav-hamburger`: `display: none;` on desktop, `display: flex;` on mobile via `@media (max-width: 768px)`
- `.bar`: `display: block; width: 24px; height: 2px; background: var(--color-text);`
- `.nav-overlay`: `position: fixed; inset: 0; z-index: 200; background: var(--color-bg); display: flex; flex-direction: column; align-items: center; justify-content: center; opacity: 0; visibility: hidden; transition: opacity 150ms ease, visibility 150ms ease;`
- `.nav-overlay.overlay--open`: `opacity: 1; visibility: visible;`
- `.overlay-links a`: `font-size: var(--font-size-lg); font-weight: var(--font-weight-medium); color: var(--color-text); text-decoration: none;`
- `.nav-close`: `position: absolute; top: var(--space-lg); right: var(--space-lg); width: var(--touch-target); height: var(--touch-target); background: none; border: none; cursor: pointer;`
- Mobile breakpoint `@media (max-width: 768px)`: `.nav-links { display: none; }` and `.nav-hamburger { display: flex; }`

Close button bars should be rotated to form an X:
- `.bar--close-1`: `position: absolute; width: 24px; height: 2px; background: var(--color-text); transform: rotate(45deg);`
- `.bar--close-2`: `position: absolute; width: 24px; height: 2px; background: var(--color-text); transform: rotate(-45deg);`

**Inline `<script>` block** — copy the COMPLETE script from PATTERNS.md Nav pattern (lines 384-430). Two features:

Feature 1 — Scroll hide/show (NAV-02):
```javascript
const nav = document.getElementById('site-nav');
let lastScrollY = window.scrollY;
const SCROLL_THRESHOLD = 80;

window.addEventListener('scroll', () => {
  const currentScrollY = window.scrollY;

  if (currentScrollY <= 0) {
    nav?.classList.remove('nav--hidden');
  } else if (currentScrollY > lastScrollY && currentScrollY > SCROLL_THRESHOLD) {
    nav?.classList.add('nav--hidden');
  } else if (currentScrollY < lastScrollY) {
    nav?.classList.remove('nav--hidden');
  }

  lastScrollY = currentScrollY;
}, { passive: true });
```
Scroll threshold: 80px. `{ passive: true }` for performance. Direction detection by comparing current scrollY to stored lastScrollY.

Feature 2 — Mobile overlay toggle (NAV-03):
```javascript
const hamburger = document.getElementById('nav-hamburger');
const overlay = document.getElementById('nav-overlay');
const closeBtn = document.getElementById('nav-close');

function openMenu() {
  overlay?.classList.add('overlay--open');
  overlay?.setAttribute('aria-hidden', 'false');
  hamburger?.setAttribute('aria-expanded', 'true');
  document.body.style.overflow = 'hidden';
}

function closeMenu() {
  overlay?.classList.remove('overlay--open');
  overlay?.setAttribute('aria-hidden', 'true');
  hamburger?.setAttribute('aria-expanded', 'false');
  document.body.style.overflow = '';
}

hamburger?.addEventListener('click', openMenu);
closeBtn?.addEventListener('click', closeMenu);

overlay?.querySelectorAll('a').forEach((link) => {
  link.addEventListener('click', closeMenu);
});
```

CRITICAL constraints:
- ALL JavaScript MUST be in an inline `<script>` block inside the .astro file — NOT in a separate src/scripts/ file, NOT imported as a module (PERF-02).
- `transition: transform 200ms ease` for scroll hide/show (UI-SPEC Interaction Contract).
- `transition: opacity 150ms ease, visibility 150ms ease` for overlay (UI-SPEC Interaction Contract).
- No decorative animations beyond nav hide/show and overlay open/close (DSGN-04).
- `aria-expanded` and `aria-hidden` toggled by script for accessibility.
  </action>
  <verify>
    <automated>cd /Users/lemondebrule/Downloads/_Claude && grep 'aria-label="Open menu"' src/components/Nav.astro && grep 'SCROLL_THRESHOLD = 80' src/components/Nav.astro && grep 'nav--hidden' src/components/Nav.astro && grep '@media (max-width: 768px)' src/components/Nav.astro && grep 'overlay--open' src/components/Nav.astro && grep 'passive: true' src/components/Nav.astro</automated>
  </verify>
  <acceptance_criteria>
    - src/components/Nav.astro contains `<nav class="site-nav" id="site-nav">`
    - src/components/Nav.astro contains `<a href="/" class="nav-logo">Alexandre Galatioto</a>`
    - src/components/Nav.astro contains `<a href="/work">Work</a>` and `<a href="/about">About</a>` in nav-links
    - src/components/Nav.astro contains `aria-label="Open menu"` on hamburger button
    - src/components/Nav.astro contains `aria-label="Close menu"` on close button
    - src/components/Nav.astro contains `aria-expanded="false"` on hamburger button
    - src/components/Nav.astro contains `aria-hidden="true"` on overlay div
    - src/components/Nav.astro contains `position: sticky` on .site-nav
    - src/components/Nav.astro contains `transition: transform 200ms ease` on .site-nav
    - src/components/Nav.astro contains `SCROLL_THRESHOLD = 80`
    - src/components/Nav.astro contains `{ passive: true }` on scroll listener
    - src/components/Nav.astro contains `.nav--hidden` class with `translateY`
    - src/components/Nav.astro contains `@media (max-width: 768px)` with `.nav-links { display: none; }` and `.nav-hamburger { display: flex; }`
    - src/components/Nav.astro contains `overlay--open` class with `opacity: 1; visibility: visible`
    - src/components/Nav.astro contains `transition: opacity 150ms ease, visibility 150ms ease` on .nav-overlay
    - src/components/Nav.astro contains `document.body.style.overflow = 'hidden'` in openMenu
    - src/components/Nav.astro contains inline `<script>` block (NOT `<script src=`)
    - src/components/Nav.astro does NOT contain `import` in the script block (no external JS modules)
  </acceptance_criteria>
  <done>Nav component renders name left, Work/About right, hides on scroll down past 80px, reappears on scroll up, shows hamburger on mobile that opens a full-screen overlay</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual verification of navigation and dev server</name>
  <action>Human verifies the nav component renders correctly in the browser, scroll hide/show works, and mobile hamburger overlay functions as specified.</action>
  <what-built>Complete Phase 1 foundation: Astro project with content collections, design tokens, global CSS, BaseLayout, Nav component with scroll hide/show and mobile hamburger overlay, and index page stub.</what-built>
  <how-to-verify>
    1. Run `cd /Users/lemondebrule/Downloads/_Claude && npm run dev` — site should start on http://localhost:4321 with zero errors
    2. Open http://localhost:4321 in a browser — you should see:
       - A top navigation bar with "Alexandre Galatioto" on the left and "Work" / "About" links on the right
       - White background, minimal design, Neue Haas Unica font
    3. Scroll down the page — the nav bar should disappear after ~80px of scrolling
    4. Scroll back up — the nav bar should reappear smoothly
    5. Open browser DevTools, toggle device toolbar to a mobile width (<=768px):
       - "Work" and "About" text links should be hidden
       - A hamburger icon (3 horizontal lines) should appear on the right
    6. Tap/click the hamburger — a full-screen white overlay should appear with "Work" and "About" centered
    7. Tap/click the X close button — the overlay should close
    8. View page source — confirm there is NO external JS bundle loaded, no analytics scripts
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Inline script execution | Nav.astro inline script runs in browser — no user input processed |
| Static HTML output | All pages pre-rendered at build time — no server-side request handling |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-04 | Spoofing | Nav links | accept | Links are hardcoded to internal routes (/work, /about) — no dynamic URLs, no user input |
| T-01-05 | Tampering | Inline script | accept | Script is static, authored by developer, no external dependencies or eval() |
| T-01-06 | Information Disclosure | Meta description | accept | Generic portfolio description — no sensitive data |
| T-01-07 | Denial of Service | Scroll event listener | mitigate | `{ passive: true }` flag on scroll listener prevents blocking main thread |
</threat_model>

<verification>
1. `npm run dev` starts without errors on http://localhost:4321
2. `npm run build` produces static output in dist/ with no JS bundle
3. Nav bar visible at page top with correct layout (name left, links right)
4. Nav hides on scroll down, reappears on scroll up
5. Mobile breakpoint shows hamburger, overlay works
6. No analytics or third-party scripts in HTML output
7. All page content inside BaseLayout's slot
</verification>

<success_criteria>
- Navigation bar renders on every page with name/logo left and Work/About links right (NAV-01)
- Nav disappears on scroll down past 80px and reappears on scroll up (NAV-02)
- Mobile hamburger menu at <=768px opens full-screen overlay (NAV-03)
- Navigation accessible on all pages via BaseLayout (NAV-04)
- No decorative animations beyond nav hide/show (DSGN-04)
- Static output mode — zero client-side JS bundle (PERF-02)
- No analytics scripts or third-party tags (PERF-03)
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-02-SUMMARY.md`
</output>
